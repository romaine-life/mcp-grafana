# mcp-grafana

Keep the MCP API compact and read-only. Discovery tools should return summaries
by default, while query tools should accept explicit limits and datasource UIDs
when callers need to override the defaults.
