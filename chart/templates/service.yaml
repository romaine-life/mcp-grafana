apiVersion: v1
kind: Service
metadata:
  name: mcp-grafana
  namespace: mcp-grafana
spec:
  type: ClusterIP
  selector:
    app: mcp-grafana
  ports:
    - name: http
      port: 80
      targetPort: proxy
